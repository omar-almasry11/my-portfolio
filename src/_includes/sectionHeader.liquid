<div>
  <h2 class="text-4xl font-bold mb-2">
    {{ header | default: "Default Header" }}
  </h2>
  {% if subheader %}
    <p class="text-2xl text-ebony mb-6">{{ subheader | default: "Default Subheader" }}</p>
  {% endif %}
</div>