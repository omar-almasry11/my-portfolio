<section class="py-20 px-[5%]">
  <div class="max-w-4xl mx-auto">
    <!-- Main Heading -->
    <h1
      id="{{ mainTitleId | default: 'pageTitle' }}"
      class="mb-6 js-colorful-heading {{ mainTitleClass | default: '' }}"
    >
      {{ headerTitle | default: 'Lorem ipsum' }}
    </h1>
    {% if headerSubtitle %}
      <!-- Supporting Text -->
      <p class="text-2xl sm:text-3xl text-normalLight max-w-2xl ml-0 dark:text-invertedLight">
        {{
          headerSubtitle
          | default: 'Anim aute id magna aliqua ad ad non deserunt sunt. Qui irure qui lorem cupidatat commodo. Elit sunt amet fugiat veniam occaecat fugiat.'
        }}
      </p>
    {% endif %}
  </div>
</section>
