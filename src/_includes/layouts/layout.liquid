<!DOCTYPE html>
  <html lang="en">
  <head>
    <!-- Basic Meta Tags -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ description | default: 'Omar Almasry Portfolio - Showcasing scalable, high-performance websites optimized for enterprises.' }}">
    <meta name="author" content="Omar Almasry">
    <title>{{ title | default: "Omar Almasry Portfolio" }}</title>
  
    <!-- Favicons -->
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
  
    <!-- Social Media Meta Tags -->
    <meta property="og:title" content="{{ title | default: 'Omar Almasry Portfolio' }}">
    <meta property="og:description" content="{{ description | default: 'Showcasing scalable, high-performance websites optimized for enterprises.' }}">
    <meta property="og:image" content="{{ og_image | default: '/images/og-image.jpg' }}">
    <meta property="og:url" content="{{ page_url | default: 'https://omaralmasry.com' }}">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ title | default: 'Omar Almasry Portfolio' }}">
    <meta name="twitter:description" content="{{ description | default: 'Showcasing scalable, high-performance websites optimized for enterprises.' }}">
    <meta name="twitter:image" content="{{ og_image | default: '/images/og-image.jpg' }}">
  
    <!-- Robots Meta -->
    <meta name="robots" content="index, follow">
  
    <!-- Stylesheet -->
    <link href="/dist/output.css" rel="stylesheet">
  
  </head>
  <body>
    {% render 'partials/navbar' %}
    <main>
      {{ content }}
    </main>
    {% render 'partials/footer' %}
  
    <!-- JavaScript -->
    <script src="/scripts/main.js" defer></script>
  </body>
  </html>